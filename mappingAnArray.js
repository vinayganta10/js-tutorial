const numbers = [1,-1,3,4];

const temp = numbers.map(value => "number = " + value);

console.log(temp);

// const items = numbers.filter(value => value>=0).map(value => "<li>" + value + "</li>");

// const html ="<ul>" + items.join("") + "</ul>";

// console.log(html);